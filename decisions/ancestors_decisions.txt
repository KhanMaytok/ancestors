targetted_decisions = {
    amaterasu = {
		only_playable = yes
		
		ai_target_filter = self
		ai = no
		
		from_potential = {
            OR = {
                trait = mangekyo_sharingan
                trait = eternal_mangekyo_sharingan
            }
            has_character_modifier = amaterasu
		}		

		effect = {
            ROOT = {
                death = { death_reason = death_amaterasu killer = FROM }
            }
            FROM = {
                character_event = { id = ACS.00407 }
            }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}

	tsukuyomi = {
		only_playable = yes
		
		ai_target_filter = self
		ai = no
		
		from_potential = {
            OR = {
                trait = mangekyo_sharingan
                trait = eternal_mangekyo_sharingan
            }
            has_character_modifier = tsukuyomi
		}		

		effect = {
            ROOT = {
                add_trait = incapable
				add_trait = lunatic
				add_trait = possessed
            }
            FROM = {
                character_event = { id = ACS.00407 }
            }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}
}